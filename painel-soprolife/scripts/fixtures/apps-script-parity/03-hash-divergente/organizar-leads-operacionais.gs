/**
 * FIXTURE SINTÉTICA — versão remota divergente do stub canônico.
 * Mantém o marcador de bloqueio, mas os bytes diferem do Git.
 */
var _OP_LEADS_BLOQUEADO =
  "FIXTURE SINTÉTICA: organização de leads bloqueada (variante remota).";

function organizarLeadsOperacionaisSoproLife() {
  throw new Error(_OP_LEADS_BLOQUEADO);
}
